interface Props {
  searchParams: {
    id: string;
  }
}
export default function MatchPage({ searchParams }: Props) {
  // const supabase = createClient();
  return <section>
    match page
  </section>
}